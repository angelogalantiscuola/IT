```mermaid
erDiagram
    world {
        name varchar(50)
        continent varchar(60)
        area decimal(10)
        population decimal(11)
        gdp decimal(14)
        capital varchar(60)
        tld varchar(5)
        flag varchar(255)
    }
```
